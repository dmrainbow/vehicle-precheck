# 项目进度记录（续作用）

> 本文件用于跨会话续作。下次继续前先读本文件 + README.md + docs/需求说明.md。

更新：2026-08-08

## 一、项目状态：功能完成，APK 已构建成功（v1 功能冻结）

- 应用功能已完成并验证：表单录入 / 预检单生成 / 分享三级降级 / 记录管理 / CSV 导出 / 设置
- **车牌识别（OCR）已废弃**（用户反馈识别不准）：改为「拍照预览」模式——摄像头取景框对准车牌 → 拍照 → 裁剪方框区域照片 → 直接预览展示 → 手动填写车牌号
- **表单字段已精简**（用户要求）：只保留 车牌号（必填）/ 变速器 / 排气管数量 / 行驶公里数，其余（车辆类型、燃油、驱动、检测线、预检项目、整车照片、备注）已删除
- 车牌照片随记录保存（`platePhoto` 字段，localStorage），预检单卡片上完整显示
- 目标交付形态：安卓 APK，经 GitHub Actions 云端打包（已成功，3 次构建后通过）

## 二、已完成

1. **应用本体**（`www/` 下，共 3 个文件：index.html / app.js / style.css）：
   - 表单录入 + Canvas 预检单卡片（750px）+ localStorage 记录 + CSV 导出 + 设置（站名/预检员）
   - 分享按钮三级降级：系统分享面板 → 微信长按引导 → 保存提示
   - 车牌照片：摄像头 + 取景框（guide）→ 裁剪 → 预览；无法调摄像头时相册兜底
   - **已删除**：Tesseract.js 本地 OCR 及 `www/lib/tesseract/`（约 12MB，减小 APK 体积）
2. **文档**：README.md、docs/需求说明.md、docs/进度记录.md（本文件）
3. **打包基础设施**：
   - `package.json`、`capacitor.config.json`（appId: com.yujian.precheck, webDir: www）
   - `.github/workflows/build-apk.yml`（Java 21 + gradle assembleDebug + 上传 APK）
   - `scripts/patch-android.mjs`（注入 CAMERA 权限 + kotlin-stdlib 排除）
4. **GitHub 已打通**（本机 MING 账号）：
   - 仓库：`https://github.com/dmrainbow/vehicle-precheck`（main）
   - 环境：Clash 代理 127.0.0.1:7897 + hosts 写 140.82.113.3 github.com + git 全局代理 + PAT（repo+workflow）
   - 构建修复记录：Java 17→21（Capacitor 要求）；kotlin-stdlib-jdk7/8 重复类（patch 追加 subprojects exclude）

## 三、进行中 / 卡点

1. **2026-08-08 大改待验证**：表单精简 + OCR 换拍照预览已完成（语法/ID 引用检查通过），尚未真机验证
2. APK 需重新构建（改动后推送触发 Actions），构建成功后在手机上验证：
   - 拍照预览（摄像头 + 方框裁剪）流程
   - 分享到微信群、离线可用

## 四、下一步（按序执行）

1. git 提交推送 → Actions 自动构建 → 下载 `app-debug.apk`
2. 手机安装 APK，真机验证：拍照取景框、照片预览、手动填车牌、分享、离线
3. 交付说明（安装方法、使用说明）

## 五、其他备忘

- 本地测试入口：静态服务器 `node C:\Users\MING\AppData\Local\Temp\opencode\static-server.js <www目录> 8765`（关机失效需重启）
- 本机环境：Windows，node v24.16.0，无 git/java/Android SDK（走云端构建）；git 走 Clash 代理
- 旧记录（2026-08-04，WBZX 账号）的 MinGit / gh CLI / 防火墙等事项已随迁移完成，不再适用
