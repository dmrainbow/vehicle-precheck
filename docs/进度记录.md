# 项目进度记录（续作用）

> 本文件用于跨会话续作。下次继续前先读本文件 + README.md + docs/需求说明.md。

更新：2026-08-04

## 一、项目状态：功能完成，APK 打包进行中

- Web 应用功能全部完成并验证通过（表单录入 / 预检单生成 / 分享三级降级 / 记录管理 / 本地 OCR 车牌识别）
- 目标交付形态：安卓 APK（用户已确认），经 GitHub Actions 云端打包

## 二、已完成

1. **应用本体**（`www/` 下）：
   - 表单录入 + Canvas 预检单卡片（750px）+ localStorage 记录 + CSV 导出 + 设置
   - 分享按钮三级降级：系统分享面板 → 微信长按引导 → 保存提示
   - 车牌识别：Tesseract.js v5.1.1 本地 OCR，`gzip:false` + v5 core 文件（simd-lstm / lstm 双核心）
   - 实测：合成车牌识别返回 `粤A12345`；分享三路径均验证通过；页面无报错
2. **文档**：README.md（项目材料）、docs/需求说明.md（需求材料）、docs/进度记录.md（本文件）
3. **打包基础设施**（已写入项目，待推送 GitHub）：
   - `package.json`、`capacitor.config.json`（appId: com.yujian.precheck, webDir: www）
   - `.github/workflows/build-apk.yml`（npm init → cap add android → patch 摄像头权限 → gradle assembleDebug → 上传 APK）
   - `scripts/patch-android.mjs`（注入 CAMERA 权限）
   - `.gitignore`
4. **目录已重构**：web 资源移入 `www/` 子目录（相对路径引用，页面已验证正常）
5. **静态服务器**：已重启指向 `C:\Users\WBZX\vehicle-precheck\www`，端口 8765（关机后失效，下次需要重新启动：`node C:\Users\WBZX\AppData\Local\Temp\opencode\static-server.js C:\Users\WBZX\vehicle-precheck\www 8765`）

## 三、进行中 / 卡点

1. **MinGit 下载中断**（关机前进行中）：
   - 目标：`https://registry.npmmirror.com/-/binary/git-for-windows/v2.55.0.windows.3/MinGit-2.55.0.3-64-bit.zip`（38.8MB）
   - 下载到 `%TEMP%\MinGit-2.55.0.3-64-bit.zip`（关机前约 9.4MB，可断点续传：`curl -C - -L -o ...`）
   - 解压到 `C:\Users\WBZX\tools\mingit`，加 PATH
2. **本机命令行无法访问 github.com**（超时）：
   - 已确认：系统代理残留配置 127.0.0.1:7890 但未启用、代理软件未运行
   - 已问用户：平时如何访问 GitHub → **用户未回答（关机）**
   - 待用户确认：开代理软件 / 网页上传代码 / 换 Gitee
3. **gh CLI**：无国内镜像（npmmirror 无 gh-cli），若走代理可从官方装；非必须（可用 PAT + curl API 代替）

## 四、下一步（开机后按序执行）

1. 续传并安装 MinGit → 验证 `git --version`
2. 向用户确认 GitHub 访问方式（代理 / 网页上传 / Gitee）
3. 推送到 GitHub（`gh repo create vehicle-precheck --public --source . --push` 或网页上传）
4. Actions 自动构建 → 下载 `app-debug.apk`（构件：precheck-apk）
5. 手机安装 APK，真机验证：OCR 摄像头、分享到微信群、离线可用
6. 交付说明（安装方法、使用说明）

## 五、其他备忘

- 防火墙 8765 端口未放行（需管理员）：`netsh advfirewall firewall add rule name="VehiclePrecheck 8765" dir=in action=allow protocol=TCP localport=8765`
- 本地测试入口：http://localhost:8765（服务器指向 www/）
- Playwright MCP 可用；测试钩子 `window.__yujianTest = { recognizePlate, prepareOcrCanvas }`
- 用户机器：Windows，node v24.16.0，无 git/java/Android SDK（所以走云端构建）
- 网络：github.com 不通；npmmirror / huaweicloud / tuna 可达（华为云约 145KB/s）
